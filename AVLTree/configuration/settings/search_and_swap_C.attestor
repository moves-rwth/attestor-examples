--description "(C) correctness: return at the root of the tree"
--classpath "configuration/code"
--class "AVLTree"
--method "searchAndSwap"
--grammar "configuration/grammar/avltree.json"
--indexed
--initial "configuration/inputs/balanced_tree.json"
--model-checking "G ({terminated} -> { return.parent == null })"
--admissible-abstraction
--no-garbage-collector
